{
  "name": "awards-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare-native federal awards data pipeline (USAspending.gov + SAM.gov bulk)",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "bootstrap": "node scripts/bootstrap.mjs",
    "bootstrap:bash": "bash scripts/bootstrap.sh",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "deploy": "pnpm -r --filter './workers/*' deploy",
    "dev:api": "pnpm --filter @awards/api dev",
    "dev:scheduler": "pnpm --filter @awards/scheduler dev",
    "dev:workflow": "pnpm --filter @awards/usaspending-workflow dev",
    "dev:sam": "pnpm --filter @awards/sam-bulk-workflow dev",
    "dev:grants": "pnpm --filter @awards/grants-gov-workflow dev",
    "dev:sam-api": "pnpm --filter @awards/sam-api dev",
    "dev:web": "pnpm --filter @awards/web dev",
    "db:create": "wrangler d1 create awards-warehouse",
    "db:migrate:local": "wrangler d1 migrations apply awards-warehouse --local --config workers/api/wrangler.toml",
    "db:migrate:remote": "wrangler d1 migrations apply awards-warehouse --remote --config workers/api/wrangler.toml",
    "r2:create": "wrangler r2 bucket create awards-staging",
    "queues:create": "wrangler queues create normalize-queue && wrangler queues create upsert-queue && wrangler queues create sam-enrich-queue && wrangler queues create dlq"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.84.1"
  }
}
