node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.local
*.log
.DS_Store
.turbo/
coverage/
.bootstrap-state
.bootstrap-state.json
*.bak

# SSH keys, cloud-provider key downloads, etc.
keys/
*.pem
*.key
id_rsa*
id_ed25519*
*-private-key*
*ssh-key*

# Temp work files (OCI CLI JSON inputs, etc.)
.oci-tmp/

# Claude Code session-local state
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Misc transient
diag-result.json
/install.ps1
NUL

# TypeScript incremental build cache
*.tsbuildinfo

# One-off operational SQL scripts (used to apply ad-hoc D1 fixes via wrangler)
.tmp-*.sql

# SLM training pipeline — data files (datasets, models, embeddings) live
# outside git for size reasons; transferred via the handoff zip instead.
slm/data/
slm/out/
slm/.venv/
slm/ssh/

# Handoff artifact — too large for git
award-lens-models.zip
awards-slm-handoff.zip

# IDE
.idea/
