{
  "name": "@awards/web",
  "version": "0.1.0",
  "private": true,
  "description": "Awards dashboard — static site deployed to Cloudflare Pages",
  "scripts": {
    "dev": "npx wrangler pages dev public --port 8788",
    "deploy": "npx wrangler pages deploy public --project-name awards-dashboard",
    "publish": "npx wrangler pages deploy public --project-name awards-dashboard"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
